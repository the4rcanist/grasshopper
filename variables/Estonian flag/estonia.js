// drawBox(blue) on the app draws a blue box
drawBox(blue);
drawBox(blue);
drawBox(blue);

// newLine() on the apps breaks the line
newLine();
// drawBox(black) on the app draws a black box
drawBox(black);
drawBox(black);
drawBox(black);

newLine();
// drawBox(white) on the app draws a white box
drawBox(white);
drawBox(white);
drawBox(white);
